import { SignalPaginatorDirective } from './signal-paginator.directive';

describe('SignalPaginatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SignalPaginatorDirective();
    expect(directive).toBeTruthy();
  });
});
